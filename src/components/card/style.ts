import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  cardStyle: {
    maxWidth: 250,
    marginTop: 24,
    marginLeft: 24,
  },
  media: {
    background: '#ff1744',
    height: 180,
  },
});
